--This SQL code below returns the total number of unique customer_id that have values greater than $5 from the payment table

SELECT 
	count (Distinct payment.customer_id)
FROM 
  payment
WHERE 
  payment.amount > 5.0
